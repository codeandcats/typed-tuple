{
  "name": "typed-tuple",
  "version": "1.0.1",
  "description": "A function that returns a typed tuple from an array of literals.",
  "main": "index.js",
  "types" : "index.d.ts",
  "scripts": {
    "build": "ts-node ./generate.ts && tsc -p ./tsconfig.json",
    "clean": "rm ./index.*",
    "prepublish": "npm run clean && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "TypeScript",
    "typed",
    "tuple",
    "type"
  ],
  "author": "Ben <codeandcats@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "10.12.12",
    "ts-node": "7.0.1",
    "typescript": "3.2.2"
  }
}
