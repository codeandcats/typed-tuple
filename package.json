{
  "name": "typed-tuple",
  "version": "1.0.0",
  "description": "Function that returns a typed tuple from an array of literals.",
  "main": "index.js",
  "types" : "index.d.ts",
  "scripts": {
    "build": "ts-node ./generate.ts && tsc -p ./tsconfig.json",
    "clean": "rm ./index.*",
    "prepublish": "npm run clean && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "TypeScript",
    "typed",
    "tuple",
    "type"
  ],
  "author": "Ben <codeandcats@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "9.4.6",
    "ts-node": "5.0.0",
    "typescript": "2.7.2"
  }
}
